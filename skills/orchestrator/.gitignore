.DS_Store
__pycache__/

.claude/

# Orchestration artefacts (runs/ and the current symlink are transient)
.orchestrator/

# Session handoff document (ephemeral per session; commit deliberately if needed)
HANDOFF.md
